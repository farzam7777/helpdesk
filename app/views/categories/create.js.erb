<% if @category.errors.blank? %>
  $('#myModal').modal('hide')
  $('#categories_section').append('<%= j render @category %>');
<% else %>
	<%= @category.errors %>
  // TODO: show the errors in the UI
<% end %>
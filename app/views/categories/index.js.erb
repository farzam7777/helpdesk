$('#categories_section').html('<%= escape_javascript(render @categories) %>')
$("#categories-count").html("Viewing <%= @categories.count %> categories (of <%= @categories.total_count %> total)")
$("#categories-paginator").html("<%= escape_javascript paginate @categories, :theme => 'twitter-bootstrap-3', remote: true, window: 2, outer_window: 3 %>")